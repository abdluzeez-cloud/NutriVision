const $=s=>document.querySelector(s);document.addEventListener("DOMContentLoaded",()=>{let f=$("#calc");if(f)f.onsubmit=async e=>{e.preventDefault();let d=Object.fromEntries(new FormData(f));let r=await fetch("/api/calculate",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(d)});let x=await r.json();let a=$("#results").querySelectorAll("strong");a[0].textContent=x.target.toLocaleString();a[1].textContent=x.bmr.toLocaleString();a[2].textContent=x.tdee.toLocaleString();a[3].textContent=x.bmi;localStorage.setItem("goal",x.target)}});function scan(){let fd=new FormData();fd.append("food",$("#food").value);fetch("/api/scan",{method:"POST",body:fd}).then(r=>r.json()).then(x=>$("#scan").innerHTML=`<div class="panel"><h2>${x.name}</h2><b>${x.calories} kcal</b> · Protein ${x.protein}g · Carbs ${x.carbs}g · Fat ${x.fat}g<br><small>Demo result — not verified for portion size.</small></div>`)}